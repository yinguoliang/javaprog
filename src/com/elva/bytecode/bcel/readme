BCEL是Apache的一款字节码操纵工具
如果说ASM是SAX之于XML，那么BCEL就是Dom之于XML

BCEL将整个class字节码加载到内存中，然后对这个字节码进行修改

BCEL提供了API可以方便的读取字节码中的信息，如方法、指令等，并对其进行了封装。

BCEL可以方便的删除现有方法、增加新方法等。

从API角度看，ASM是非常原始的，BCEL的功能比ASM要强大一些，使用起来也更方便一些。
当然，BCEL仍然是一个底层字节码工具，如果要使用更高级的功能，可以使用javasist等工具。